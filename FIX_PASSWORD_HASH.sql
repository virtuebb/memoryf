-- =====================================================
-- 비밀번호 해시 수정 스크립트
-- =====================================================
-- 
-- 문제: DB에 저장된 BCrypt 해시가 "1234"와 일치하지 않음
-- 해결: 올바른 BCrypt 해시로 업데이트
--
-- 참고: 이 해시는 Spring Security BCryptPasswordEncoder로 생성된 "1234"의 해시입니다.
--       BCrypt는 매번 다른 해시를 생성하므로, 실제 애플리케이션에서 생성한 해시를 사용해야 합니다.
--
-- =====================================================

-- 방법 1: 임시로 평문 비밀번호를 직접 해시하여 업데이트 (권장하지 않음)
-- 방법 2: 비밀번호 재설정 API를 통해 새로운 해시 생성 (권장)

-- 방법 3: 테스트용 - 올바른 BCrypt 해시로 업데이트
-- 주의: 이 해시는 BCryptPasswordEncoder로 "1234"를 인코딩한 결과입니다.
--       실제 환경에서는 비밀번호 재설정 기능을 사용하세요.

-- testuser 비밀번호를 "1234"의 올바른 BCrypt 해시로 업데이트
-- 이 해시는 Spring Boot 애플리케이션에서 생성해야 합니다.
-- 아래는 예시이며, 실제로는 애플리케이션에서 생성한 해시를 사용해야 합니다.

-- UPDATE TB_MEMBER 
-- SET MEMBER_PWD = '$2a$10$[새로운해시값]'
-- WHERE MEMBER_ID = 'testuser';

-- =====================================================
-- 권장 방법: 비밀번호 재설정 API 사용
-- =====================================================
-- POST /auth/reset-password
-- Body: { "memberId": "testuser", "memberPwd": "1234" }
--
-- 또는 회원가입을 통해 새 계정 생성
-- =====================================================

COMMIT;

