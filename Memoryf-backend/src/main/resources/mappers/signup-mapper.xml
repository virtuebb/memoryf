<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="signupMapper">
	
	<insert id="insertMember" parameterType="signup" useGeneratedKeys="true" keyProperty="memberNo" keyColumn="MEMBER_NO">
        INSERT INTO TB_MEMBER (
        		MEMBER_NO,
            MEMBER_ID,
            MEMBER_PWD,
            MEMBER_NAME,
            MEMBER_NICK,
            EMAIL,
            PHONE,
            GENDER,
            BIRTHDAY,
            STATUS
        ) VALUES (
        		SEQ_MEMBER_NO.NEXTVAL,
            #{memberId},
            #{memberPwd},
            #{memberName},
            #{memberNick},
            #{email},
            #{phone},
            #{gender},
            #{birthday},
            'N'
        )	
	</insert>
	
	<insert id="insertHome" parameterType="signup">
		INSERT INTO TB_MEMBER_HOME (
			HOME_NO,
			MEMBER_NO,
			HOME_TITLE
		) VALUES (
			SEQ_HOME_NO.NEXTVAL,
			#{memberNo},
			#{memberNick} || '님의 미니홈피'
		)
	</insert>
	
	<select id="checkMemberId" parameterType="string" resultType="int">
		SELECT COUNT(*)
		  FROM TB_MEMBER
		 WHERE MEMBER_ID = #{memberId}
	</select>
	
	<select id="checkMemberNick" parameterType="string" resultType="int">
		SELECT COUNT(*)
		  FROM TB_MEMBER
		 WHERE MEMBER_NICK = #{memberNicj}
	</select>
</mapper>